/**
 * Camera and model error handling constants
 */

// Maximum consecutive model inference errors before attempting fallback
export const MAX_MODEL_ERRORS_BEFORE_FALLBACK = 3;

// Pause duration (in milliseconds) after model errors before retrying
export const MODEL_ERROR_PAUSE_MS = 1000;
